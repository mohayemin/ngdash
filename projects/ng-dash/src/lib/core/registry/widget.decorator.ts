import { createRegistryDecorator } from './create-registry-decorator';

/**
 * Register a component as a widget
 * @param id a unique ID to identify the widget
 */
export const NgDashWidget = createRegistryDecorator();
