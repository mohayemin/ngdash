import { createRegistryDecorator } from './create-registry-decorator';

/**
 * Register a component as a widget header
 * @param id a unique ID to identify the widget header
 */
export const NgDashWidgetHeader = createRegistryDecorator();
