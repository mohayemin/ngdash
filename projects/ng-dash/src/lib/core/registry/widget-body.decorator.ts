import { createRegistryDecorator } from './create-registry-decorator';

/**
 * Register a component as a widget body
 * @param id a unique ID to identify the widget body
 */
export const NgDashWidgetBody = createRegistryDecorator();
