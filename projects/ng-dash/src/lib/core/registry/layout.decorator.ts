import { createRegistryDecorator } from './create-registry-decorator';

/**
 * Register a component as a layout
 * @param id a unique ID to identify the layout
 */
export const NgDashLayout = createRegistryDecorator();